{
  "name": "taketime",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "desktop:icons": "cd desktop && npm run generate-icons",
    "desktop:build": "npm run build && cd desktop && npm run tauri:build",
    "desktop:dev": "cd desktop && npm run tauri:dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0",
    "animal-island-ui": "^0.9.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/uuid": "^9.0.7",
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.1.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
